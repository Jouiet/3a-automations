/**
 * Remotion Entry Point - 3A Automation Studio
 * Registers the Root component with all compositions
 */
import { registerRoot } from 'remotion';
import { Root } from './Root';

registerRoot(Root);
