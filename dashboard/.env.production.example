# 3A Automation Dashboard - Production Environment
# Copy this file to .env.production on the VPS and fill in values
# NEVER commit the actual .env.production file

# Google Sheets Integration
GOOGLE_SHEETS_ID=your_google_sheets_id_here
GOOGLE_SHEETS_API_URL=https://script.google.com/macros/s/your_script_id/exec

# Authentication (MUST be rotated after exposure)
JWT_SECRET=generate_new_secret_with_openssl_rand_base64_32

# n8n Integration (MUST be rotated after exposure)
N8N_API_KEY=generate_new_api_key_from_n8n_dashboard
